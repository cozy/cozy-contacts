{
  "name": "cozy-contacts",
  "version": "0.1.0",
  "scripts": {
    "clean:browser": "rm -rf build/*",
    "tx": "tx pull --all || true",
    "lint": "npm-run-all --parallel 'lint:*'",
    "lint:js":
      "prettier --write '{src,test}/**/*.{js,jsx}' && eslint --fix '{src,test}/**/*.{js,jsx}'",
    "lint:styles": "stylint src/styles --config ./.stylintrc",
    "build": "yarn run build:browser",
    "commons:build": "cozy-scripts build",
    "build:browser":
      "NODE_ENV=browser:production npm-run-all --serial clean:browser lint:js tx commons:build",
    "watch": "yarn run watch:browser",
    "commons:watch": "cozy-scripts watch",
    "watch:browser":
      "NODE_ENV=browser:development npm-run-all --serial clean:browser commons:watch",
    "watch:standalone": "yarn run watch:browser:standalone",
    "commons:watch:standalone": "cozy-scripts standalone",
    "watch:browser:standalone":
      "NODE_ENV=browser:development yarn run commons:watch:standalone",
    "config:browser:production":
      "NODE_ENV=browser:production cozy-scripts --show-config",
    "config:browser:development":
      "NODE_ENV=browser:development cozy-scripts --show-config",
    "deploy":
      "git-directory-deploy --username cozy --email contact@cozycloud.cc --directory build/ --repo=https://$GITHUB_TOKEN@github.com/cozy/cozy-contacts.git",
    "test": "npm-run-all --serial lint test:jest",
    "test:jest": "jest --verbose --coverage",
    "stack:docker":
      "docker run --rm -it -p 8080:8080 -v \"$(pwd)/build\":/data/cozy-app/app cozy/cozy-app-dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cozy/cozy-contacts.git"
  },
  "author": "",
  "contributors": [],
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/cozy/cozy-contacts/issues"
  },
  "homepage": "https://github.com/cozy/cozy-contacts#readme",
  "devDependencies": {
    "babel-core": "6.26.0",
    "babel-jest": "22.1.0",
    "babel-polyfill": "6.26.0",
    "babel-preset-cozy-app": "0.3.1",
    "enzyme": "3.3.0",
    "enzyme-adapter-react-16": "1.1.1",
    "eslint": "4.15.0",
    "eslint-config-cozy-app": "0.3.1",
    "git-directory-deploy": "1.5.1",
    "identity-obj-proxy": "3.0.0",
    "jest": "22.1.1",
    "npm-run-all": "4.1.2",
    "prettier": "1.10.2",
    "react": "16.2.0",
    "react-dom": "16.2.0",
    "react-test-renderer": "16.2.0",
    "redux-logger": "3.0.6",
    "redux-mock-store": "1.4.0",
    "stylint": "1.5.9"
  },
  "dependencies": {
    "cozy-bar": "4.8.2",
    "cozy-client-js": "0.4.5",
    "cozy-scripts": "0.4.1",
    "cozy-ui": "7.0.3",
    "preact": "8.2.7",
    "preact-compat": "3.17.0"
  }
}
